public enum ChallengeGoalType
{
	/// <summary>Defeat the tier's final boss.</summary>
	KillAllBosses,
	/// <summary>Reach the final boss phase (10:00 mark in current run ruleset).</summary>
	ReachFinalSwarm
}
